= render 'layouts/header'
- breadcrumb :user_logout, current_user
.breadcrumbs
  = breadcrumbs separator: "#{fa_icon("angle-right", class: "breadcrumbs__seperator")} ", class: "breadcrumbs__container"
.wrapper
  = render 'sidebar'

  .sign-out
    .sign-out__btn
      = link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "sign-out__btn"
= render 'layouts/footer'
