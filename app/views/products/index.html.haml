= render 'layouts/header'
%main.products-field
  .pickup-products-container
    %h2.pickup-head
      ピックアップカテゴリー
    = render partial: "category", collection: @categories, as: 'category'

  .pickup-products-container
    %h2.pickup-head
      ピックアップブランド
    = render partial: "brand", collection: @brands, as: 'brand'
= render 'layouts/footer'
= render 'layouts/sellform_link_button'

